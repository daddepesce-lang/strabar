<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              App Links: i link https://strabar.app (itinerari condivisi, profili, eventi,
              reset password) aprono direttamente l'app invece del browser.
              `autoVerify` fa controllare ad Android /.well-known/assetlinks.json sul dominio:
              serve che ANDROID_CERT_SHA256 su Vercel contenga le impronte SHA-256 giuste,
              altrimenti la verifica non passa e i link restano nel browser.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="strabar.app" />
                <data android:scheme="https" android:host="www.strabar.app" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
          Aspetto delle notifiche push in arrivo da FCM: senza questi due meta-data Android
          mostrerebbe un quadrato bianco al posto dell'icona. `ic_stat_icon` è la silhouette
          monocromatica del marchio (res/drawable-*/ic_stat_icon.png), la tinta è l'ember red.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_icon" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!--
      Le altre permission arrivano dal merge dei plugin:
      POST_NOTIFICATIONS (push/local-notifications), ACCESS_FINE/COARSE_LOCATION
      (geolocation: le usa anche navigator.geolocation dentro la WebView),
      CAMERA e accesso alle foto (camera).
    -->
</manifest>
