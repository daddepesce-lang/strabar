// Canonical proprio della pagina Business (locali): pagina pubblica indicizzabile.
export const metadata = {
  title: 'Strabar per i Locali',
  alternates: { canonical: '/business' },
};

export default function BusinessLayout({ children }) {
  return children;
}
