// Canonical proprio della pagina Installa/Invita: pagina pubblica indicizzabile.
export const metadata = {
  title: 'Installa Strabar',
  alternates: { canonical: '/install' },
};

export default function InstallLayout({ children }) {
  return children;
}
